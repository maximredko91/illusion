package com.illusion.app.domain.model

/** BLACK is DARK with pure black (#000000) backgrounds/surfaces instead of Material's usual dark-grey tones - true AMOLED black for the power/contrast some users specifically want, not just "extra dark". */
enum class ThemeMode {
    SYSTEM,
    LIGHT,
    DARK,
    BLACK
}
